#include <bits/stdc++.h>
#define _ std::ios_base::sync_with_stdio(false);std::cin.tie(NULL);

int main(){
    _
    int a, b, c, d;
    std::cin >> a >> b >> c >> d;
    std::cout << "DIFERENCA = " << a*b - c*d << "\n";
    return 0;
}